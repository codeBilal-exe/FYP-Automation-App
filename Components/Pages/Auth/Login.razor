@page "/login"
@inject NavigationManager Nav

@code {
  protected override void OnInitialized()
  {
    var uri = new Uri(Nav.Uri);
    var target = string.IsNullOrWhiteSpace(uri.Query) ? "/" : $"/{uri.Query}";
    Nav.NavigateTo(target, forceLoad: true);
  }
}
